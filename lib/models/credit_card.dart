class CreditCard{

   final String cardNumber,cvv,scheme,bank,type,branch,bankEmoji;

   CreditCard({required this.cardNumber,
     required this.cvv, required this.scheme,
     required this.bank, required this.type,
      required this.branch,required this.bankEmoji});
}